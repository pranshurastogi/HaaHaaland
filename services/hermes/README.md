# Optional private Hermes VAR service

P1 only. Deploy as a private Railway service with no public domain. The public API may send only validated card fields and a short appeal reason over Railway private networking. Never enable terminal/filesystem tools, generic chat, or user-supplied agent instructions. Keep `HERMES_RUNTIME_ENABLED=false` until the private endpoint and authentication are tested.
